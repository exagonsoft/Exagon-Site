version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
        - npm run export
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
  runtime:
    # Specify the Node.js version
    runtime: nodejs
    version: 20.5.0
